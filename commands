1.Initializing a new repository:
bash
git init

2.Clone a repository:
bash
git clone <repository_url>

3.Check the status of your repository:
bash
git status

4.Stage changes for commit:
bash
git add <file_name>

5.To stage all changes:
bash
git add .
  
6.Commit changes:
bash
git commit -m "Your commit message"
  
7.View commit history:
bash
git log

8.Create a new branch:
bash
git branch <branch_name>

9.Switch to a branch:
bash
git checkout <branch_name>
Or, using newer Git versions:
bash
git switch <branch_name>
  
9.Merge branches:
bash
git merge <branch_name>

10.Fetch changes from a remote repository:
bash
git fetch

11.Pull changes from a remote repository:
bash
git pull

12.Push changes to a remote repository:
bash
git push origin <branch_name>

13.Show changes between commits:
bash
git diff

14.Discard changes in working directory:
bash
git restore <file_name>

15.To discard all changes:
bash
git restore .
  
16.Configure user information:
bash
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
  
17.Create and apply patches:
bash
git diff > patchfile    # Create a patch
git apply < patchfile   # Apply a patch

18.Delete the file
bash
git rm filename
git add .
git commit -m our message
git push origin branch-name 
